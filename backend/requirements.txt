# Dependensi runtime BACKEND SIPREBAR.
#
# Sengaja DIPISAH dari kebutuhan pipeline training penuh
# (audit/source_fixed/*.py juga butuh seaborn/statsmodels/dst. untuk
# eksplorasi & training -- backend TIDAK PERNAH melatih model, hanya
# memuat artefak .keras/.pkl yang sudah ada lewat tensorflow/scikit-learn).
#
# Install:  pip install -r backend/requirements.txt

fastapi==0.115.6
uvicorn[standard]==0.34.0
SQLAlchemy==2.0.36
pandas>=2.2
pydantic>=2.9
python-multipart==0.0.20
openpyxl>=3.1

# Dipakai backend/cuaca.py untuk menarik observasi cuaca bulanan dari
# NASA POWER (power.larc.nasa.gov) saat bulan target sudah lewat. Kalau
# panggilan gagal, sistem jatuh ke normal klimatologis dari database --
# jadi ketiadaan internet tidak mematikan halaman Prediksi.
requests>=2.32

# Untuk memuat model_final_*.keras & scaler_*.pkl (Task Group 7). Versi
# mengikuti apa yang sudah dipakai saat model ini dilatih oleh pipeline.
tensorflow==2.19.0
scikit-learn==1.6.1
